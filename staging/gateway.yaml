apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
  name: gateway
  namespace: staging
spec:
  rules:
  - host: staging.devheaven.nl
    http:
      paths:
      - backend:
          serviceName: frontend-service
          servicePort: 80
        path: /