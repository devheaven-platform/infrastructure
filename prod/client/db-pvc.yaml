apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: client-mongo-pvc
  namespace: production
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs